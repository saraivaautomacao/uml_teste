unit limitecredito;

interface

uses
  Windows, Messages, SysUtils, Classes, Graphics, Controls, Forms, Dialogs,
  StdCtrls, Mask, rxrxToolEdit, rxrxCurrEdit;

type
  TFormLimCredito = class(TForm)
    Label1: TLabel;
    fldValor1: TCurrencyEdit;
    fldValor2: TCurrencyEdit;
    Label2: TLabel;
    Button1: TButton;
    Button2: TButton;
    procedure FormShow(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FormLimCredito: TFormLimCredito;

implementation

{$R *.DFM}

procedure TFormLimCredito.FormShow(Sender: TObject);
begin
 fldValor2.setFocus;
end;

end.
